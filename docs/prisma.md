# Prisma

## Aliases

```sh
alias pm="prisma migrate dev --name"              # Create and apply a migration
alias pmd="prisma migrate dev --create-only --name"  # Create a migration without applying
```

